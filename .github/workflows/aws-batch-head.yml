name: deploy_aws_batch_head_docker_image

on:
  push:
    branches:
      - gh-action
env:
  IMG_TAG: 1.0
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Docker
        uses: docker/setup-buildx-action@v1
      - name: Login to Quay.io
        uses: docker/login-action@v2
        with:
          registry: quay.io
          # username: ${{ secrets.QUAY_USERNAME }}
          # password: ${{ secrets.QUAY_PASSWORD }}
      - name: Build and push
        uses: docker/build-push-action@v2
        with:
          push: true
          context: .
          file: aws-batch/Dockerfile
          tags: quay.io/enasequence/ena-sars-cov2-aws-batch-head:${{ env.IMG_TAG }}
      # - name: Build the image
      #   run: |
      #     docker build -t quay.io/enasequence/ena-sars-cov2-aws-batch-head:$IMG_TAG .
      # - name: Push the image
      #   run: |
      #     docker push quay.io/enasequence/ena-sars-cov2-aws-batch-head:$IMG_TAG
